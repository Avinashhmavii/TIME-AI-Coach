import { InterviewSession } from "@/components/interview-session";

export default function InterviewPage() {
    return (
        <div className="w-full h-screen bg-gray-50/50">
            <InterviewSession />
        </div>
    )
}
